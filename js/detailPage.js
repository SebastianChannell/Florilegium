import { getLiturgicalDashboardData } from './liturgicalDataProvider.js';
function setupMenu(){const sideMenu=document.querySelector('.side-menu');const toggle=document.querySelector('.menu-toggle');const panel=document.getElementById('siteMenu');function setOpen(open){sideMenu?.classList.toggle('is-open',open);toggle?.setAttribute('aria-expanded',String(open));panel?.setAttribute('aria-hidden',String(!open));}sideMenu?.addEventListener('click',(event)=>{if(event.target.closest('.menu-toggle'))setOpen(!sideMenu.classList.contains('is-open'));if(event.target.closest('.menu-close, .menu-backdrop, .menu-link'))setOpen(false);});window.addEventListener('keydown',(event)=>{if(event.key==='Escape')setOpen(false);});}
function entry(title, text){return text ? `<section class="proper-section"><h2>${title}</h2><p>${String(text).replace(/\n/g,'<br>')}</p></section>` : '';}
async function init(){setupMenu();const data=await getLiturgicalDashboardData();const root=document.getElementById('detailContent');if(!root)return;if(root.dataset.page==='ordo'){root.innerHTML=`<article class="sf-card detail-card"><p class="sf-label">Ordo</p><h1>${data.today.title}</h1><p class="ordo-lines">${data.ordo.summaryLines.join('\n')}</p><div class="proper-section"><p>${String(data.ordo.fullText).replace(/\n/g,'<br>')}</p></div></article>`;return;}const p=data.readings.propers;root.innerHTML=`<article class="sf-card detail-card"><p class="sf-label">Mass Propers</p><h1>${data.readings.title}</h1><p class="sf-muted">${data.readings.references.join(' · ')}</p>${entry('Introit',p.introit)}${entry('Collect',p.collect)}${entry('Epistle / Lesson',p.epistle)}${entry('Gradual',p.gradual)}${entry('Alleluia / Tract',p.alleluia||p.tract)}${entry('Gospel',p.gospel)}${entry('Offertory',p.offertory)}${entry('Secret',p.secret)}${entry('Preface',p.preface)}${entry('Communion',p.communion)}${entry('Postcommunion',p.postcommunion)}${entry('Commemorations',p.commemorations)}</article>`;}
window.addEventListener('DOMContentLoaded',init);
